const { config } = require('./dist/config').default;

module.exports = {
    migrationPath: 'migrations',
    dbms: 'mysql',
    connection: {...config.db.master,
        flags: {ghost: config.ghost}
    },
    migrationTableName: '_migrations'
};
