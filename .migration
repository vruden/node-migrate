var path = require('path');

// Path to the migrations directory (relative to migrate)
exports.migrationPath = path.join(__dirname, './migrations/');

// Which DBMS to use for executing migrations
exports.dbms = 'mysql';

// Configuration for MySQL (username, password, etc.)
exports.connection = {
    user: 'root',
    password: '123456',
    database: 'node-test'
};

exports.migrationTableName = '_migrations';